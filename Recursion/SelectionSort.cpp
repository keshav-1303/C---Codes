#include <bits/stdc++.h>
using namespace std;

void SelectionSort(int *arr, int n){

    if(n==0 || n==1)    return ;

    for(int ))
}

int main(){

	int arr[] = {1,3,2,4,5,8,6,4,9,3,4};
    int n = sizeof(arr)/sizeof(arr[0]);

    SelectionSort(arr, n);

	return 0;
}